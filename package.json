{
  "name": "archivision-ai",
  "version": "1.0.0",
  "private": true,
  "description": "ArchiVision AI — Plateforme IA d'Architecture et Génération de Plans",
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "cd server && npm run build && cd ../client && npm run build",
    "install:all": "cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^6.0.3"
  }
}
